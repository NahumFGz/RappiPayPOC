export const accountBasePath = '/account'

export const accountUrls = {
  base: accountBasePath,

  // accountSettings
  profile: `${accountBasePath}/profile`,
  security: `${accountBasePath}/security`
}
